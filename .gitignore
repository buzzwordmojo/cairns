node_modules/
dist/
*.tsbuildinfo

# derived state — never committed, or the board invents merge conflicts
.tasks/.index
.tasks/.cache/

.env
.env.*
.DS_Store
*.log

# a publish token in the tree is one `git add -A` away from the registry
npm-token*
